# 看板娘按钮功能说明

## 🎮 新增的自定义按钮

我为看板娘添加了 10 个实用且有趣的自定义按钮，每个按钮都有独特的功能：

---

### 1. ⬆️ 回到顶部
- **功能**：平滑滚动回到页面顶部
- **提示**：咻~ 回到顶部啦！
- **使用场景**：浏览长文章时快速返回顶部

### 2. 🎲 随机访问
- **功能**：随机跳转到网站的其他页面
- **提示**：让我带你去一个随机的地方~
- **使用场景**：探索网站内容，增加趣味性

### 3. 📤 分享页面
- **功能**：分享当前页面（支持原生分享或复制链接）
- **提示**：分享给你的朋友吧~
- **使用场景**：分享有趣的内容给朋友

### 4. 🎵 音乐播放
- **功能**：切换音乐播放状态（可扩展实际播放功能）
- **提示**：开始播放音乐~ / 音乐已暂停
- **图标变化**：🎵 ↔️ 🎶
- **使用场景**：背景音乐控制

### 5. 🌓 主题切换
- **功能**：快速切换明暗主题
- **提示**：切换主题模式~
- **使用场景**：快捷切换主题，无需点击顶部按钮

### 6. 📸 截图保存
- **功能**：截取看板娘的照片
- **提示**：咔嚓~ 照片拍好了！
- **使用场景**：保存看板娘的可爱瞬间

### 7. 📊 访问统计
- **功能**：显示访问次数和首次访问时间
- **提示**：显示详细统计信息
- **使用场景**：查看个人访问记录

### 8. 🔧 开发者工具
- **功能**：在控制台输出调试信息
- **提示**：打开控制台看看吧~
- **使用场景**：开发者调试，查看内部状态

### 9. 🗑️ 清除缓存
- **功能**：清除所有本地存储数据
- **提示**：缓存已清除！
- **使用场景**：重置访问记录和设置

### 10. ❓ 帮助
- **功能**：显示快捷键和使用提示
- **提示**：显示帮助信息
- **使用场景**：新用户了解功能

---

## 🎨 按钮样式特点

### 视觉效果
- ✨ 毛玻璃背景效果
- 🎯 圆形按钮设计
- 🌈 渐变色悬停效果
- 💫 平滑动画过渡
- 🎭 支持明暗主题

### 交互效果
- **悬停**：放大 + 旋转 + 渐变色
- **点击**：缩小反馈
- **阴影**：动态阴影效果

### 响应式
- 自动换行排列
- 移动端自适应隐藏

---

## 🔧 自定义按钮

### 添加新按钮

在 `public/live2d-custom-config.js` 的 `addCustomButtons` 函数中添加：

```javascript
addButton(toolsContainer, {
  icon: '🚀',              // 按钮图标（Emoji）
  title: '按钮标题',       // 悬停提示
  message: '点击后的消息', // 看板娘说的话
  onClick: () => {
    // 你的自定义功能
    console.log('按钮被点击了！');
  }
});
```

### 示例：添加"返回首页"按钮

```javascript
addButton(toolsContainer, {
  icon: '🏠',
  title: '返回首页',
  message: '带你回家~',
  onClick: () => {
    window.location.href = '/';
  }
});
```

### 示例：添加"全屏"按钮

```javascript
addButton(toolsContainer, {
  icon: '⛶',
  title: '全屏模式',
  message: '进入全屏模式~',
  onClick: () => {
    if (!document.fullscreenElement) {
      document.documentElement.requestFullscreen();
    } else {
      document.exitFullscreen();
    }
  }
});
```

---

## 💡 高级功能示例

### 1. 带状态切换的按钮

```javascript
let isDarkMode = false;
const themeBtn = addButton(toolsContainer, {
  icon: '☀️',
  title: '切换主题',
  message: '',
  onClick: () => {
    isDarkMode = !isDarkMode;
    themeBtn.innerHTML = isDarkMode ? '🌙' : '☀️';
    // 切换主题逻辑
  }
});
```

### 2. 带计数器的按钮

```javascript
let clickCount = 0;
addButton(toolsContainer, {
  icon: '👆',
  title: '点击计数',
  message: '',
  onClick: () => {
    clickCount++;
    window.showMessage(`你已经点击了 ${clickCount} 次！`, 3000);
  }
});
```

### 3. 带确认对话框的按钮

```javascript
addButton(toolsContainer, {
  icon: '⚠️',
  title: '危险操作',
  message: '',
  onClick: () => {
    if (confirm('确定要执行此操作吗？')) {
      window.showMessage('操作已执行！', 3000);
      // 执行操作
    }
  }
});
```

### 4. 异步操作按钮

```javascript
addButton(toolsContainer, {
  icon: '📡',
  title: '获取数据',
  message: '正在获取数据...',
  onClick: async () => {
    try {
      const response = await fetch('/api/data');
      const data = await response.json();
      window.showMessage(`获取成功：${data.message}`, 3000);
    } catch (error) {
      window.showMessage('获取失败！', 3000);
    }
  }
});
```

---

## 🎯 实用功能建议

### 社交媒体分享
```javascript
addButton(toolsContainer, {
  icon: '🐦',
  title: '分享到 Twitter',
  message: '分享到 Twitter~',
  onClick: () => {
    const url = encodeURIComponent(window.location.href);
    const text = encodeURIComponent(document.title);
    window.open(`https://twitter.com/intent/tweet?url=${url}&text=${text}`);
  }
});
```

### 打印页面
```javascript
addButton(toolsContainer, {
  icon: '🖨️',
  title: '打印页面',
  message: '准备打印~',
  onClick: () => {
    window.print();
  }
});
```

### 字体大小调整
```javascript
let fontSize = 16;
addButton(toolsContainer, {
  icon: '🔍',
  title: '放大字体',
  message: '字体变大了~',
  onClick: () => {
    fontSize += 2;
    document.body.style.fontSize = fontSize + 'px';
  }
});
```

### 阅读进度
```javascript
addButton(toolsContainer, {
  icon: '📖',
  title: '阅读进度',
  message: '',
  onClick: () => {
    const percent = Math.round(
      (window.scrollY / (document.body.scrollHeight - window.innerHeight)) * 100
    );
    window.showMessage(`当前阅读进度：${percent}%`, 3000);
  }
});
```

---

## 🎨 样式自定义

### 修改按钮大小
在 `Layout.astro` 的样式中修改：

```css
.waifu-tool span {
  width: 40px !important;   /* 改变宽度 */
  height: 40px !important;  /* 改变高度 */
  font-size: 20px !important; /* 改变图标大小 */
}
```

### 修改按钮颜色
```css
.waifu-tool span:hover {
  background: linear-gradient(135deg, #your-color-1, #your-color-2) !important;
}
```

### 修改工具栏位置
```css
.waifu-tool {
  position: fixed;
  bottom: 100px;  /* 调整位置 */
  right: 20px;
}
```

---

## 📱 移动端适配

### 在移动端显示部分按钮
```css
@media (max-width: 768px) {
  .waifu-tool span:nth-child(n+6) {
    display: none !important; /* 隐藏第6个之后的按钮 */
  }
}
```

---

## 🐛 故障排除

### 按钮不显示？
1. 检查控制台是否有错误
2. 确认 `live2d-custom-config.js` 已正确加载
3. 等待 3 秒让看板娘完全加载

### 按钮功能不工作？
1. 检查 `window.showMessage` 是否可用
2. 查看浏览器控制台的错误信息
3. 确认按钮的 onClick 函数没有语法错误

### 样式不正确？
1. 清除浏览器缓存
2. 检查 CSS 优先级（使用 `!important`）
3. 确认主题切换后样式是否正常

---

## 🎉 总结

现在你的看板娘拥有了：
- ✅ 10+ 实用功能按钮
- ✅ 美观的毛玻璃效果
- ✅ 流畅的动画交互
- ✅ 完整的明暗主题支持
- ✅ 易于扩展的架构

**尽情享受你的智能看板娘吧！** 💖
